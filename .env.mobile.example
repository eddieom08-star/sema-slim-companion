# Mobile Environment Configuration for Build Time
# Copy this to .env.mobile for mobile-specific builds
# IMPORTANT: Only VITE_ prefixed variables are embedded in the mobile app
# Server-side secrets (AUTH0_*, DATABASE_URL) must ONLY be on your backend server

# Backend API URL (embedded in mobile app)
# For development with mobile emulator/device, use your machine's IP
# For production, use your deployed backend URL
VITE_API_URL=http://192.168.1.xxx:5000

# App Configuration (embedded in mobile app)
VITE_APP_NAME=SemaSlim
VITE_APP_VERSION=1.0.0

# NOTE: Your backend server should have these environment variables:
# - AUTH0_DOMAIN
# - AUTH0_CLIENT_ID
# - AUTH0_CLIENT_SECRET
# - SESSION_SECRET
# - DATABASE_URL
# These are NEVER included in the mobile build for security reasons.
